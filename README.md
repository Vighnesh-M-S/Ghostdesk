# 👻 GhostDesk — Privacy-First AI Support Employee

> Humans should not spend time reading support requests. They should only make decisions when judgment is required.

GhostDesk is an AI-powered support automation system that reads customer emails, redacts PII before AI processing, analyzes attachments, verifies claims, assesses risk, and recommends actions — all through a LangGraph multi-agent workflow.

---

## Architecture

```
Email + Attachments
        │
        ▼
┌─────────────────────────────────────────────────────────┐
│                   LangGraph Workflow                     │
│                                                         │
│  EMAIL_PARSER → PII_REDACTION → INTENT_CLASSIFICATION  │
│       → ATTACHMENT_ANALYSIS → VERIFICATION              │
│       → RISK_DETECTION → RESOLUTION → HUMAN_APPROVAL   │
└─────────────────────────────────────────────────────────┘
        │
        ▼
  Final Recommendation
```

| Agent | Role |
|-------|------|
| `EMAIL_PARSER` | Normalize raw email text |
| `PII_REDACTION` | Redact names, phones, emails, cards using Presidio |
| `INTENT_CLASSIFICATION` | Classify intent with Gemini (7 categories) |
| `ATTACHMENT_ANALYSIS` | Extract text from PDFs/images, generate summaries |
| `VERIFICATION` | Compare email claims against attachment evidence |
| `RISK_DETECTION` | Detect fraud signals, mismatches, suspicious patterns |
| `RESOLUTION` | Recommend action (Approve / Create Jira / Escalate / etc.) |
| `HUMAN_APPROVAL` | Trigger human review when confidence < 80% or risk > 70% |

---

## Tech Stack

| Layer | Technology |
|-------|-----------|
| Frontend | Streamlit |
| Backend | FastAPI |
| Agent Framework | LangGraph |
| LLM | Gemini 1.5 Flash (Google) |
| PII Protection | Microsoft Presidio |
| Document Processing | pdfplumber, PyMuPDF, pytesseract |
| Database | SQLite |
| Observability | LangSmith-ready |

---

## Setup

### 1. Clone and install dependencies

```bash
python -m venv .venv
source .venv/bin/activate        # Windows: .venv\Scripts\activate

pip install -r requirements.txt
```

### 2. Download spaCy model (required by Presidio)

```bash
python -m spacy download en_core_web_lg
```

### 3. Install Tesseract OCR (for image attachments)

- **macOS**: `brew install tesseract`
- **Ubuntu**: `sudo apt-get install tesseract-ocr`
- **Windows**: [Download installer](https://github.com/UB-Mannheim/tesseract/wiki)

### 4. Configure environment

```bash
cp .env.example .env
# Edit .env and add your GEMINI_API_KEY
```

Get a Gemini API key at: https://aistudio.google.com/apikey

### 5. Generate sample PDF attachments (optional)

```bash
python sample_data/generate_sample_pdfs.py
```

---

## Running the Application

Open **two terminals**:

**Terminal 1 — Backend (FastAPI)**
```bash
source .venv/bin/activate
uvicorn backend.main:app --reload --host 0.0.0.0 --port 8000
```

**Terminal 2 — Frontend (Streamlit)**
```bash
source .venv/bin/activate
streamlit run frontend/app.py
```

Open your browser at **http://localhost:8501**

API docs available at **http://localhost:8000/docs**

---

## Demo Scenarios

Three sample emails are included in `sample_data/test_emails/`:

| Scenario | Intent | Expected Action |
|----------|--------|-----------------|
| `refund_request.txt` | `REFUND_REQUEST` | `APPROVE_REFUND` |
| `technical_issue.txt` | `TECHNICAL_ISSUE` | `CREATE_JIRA` |
| `fraud_scenario.txt` | `REFUND_REQUEST` | `ESCALATE_SUPPORT` (high risk) |

For the fraud scenario, pair with `sample_data/test_attachments/charge_receipt.pdf` (generated by the script above) to see the MISMATCH verification in action.

---

## API Reference

| Method | Endpoint | Description |
|--------|----------|-------------|
| `POST` | `/process` | Process email (form: `email_text`, files: `attachments`) |
| `GET` | `/analytics` | Aggregated metrics |
| `GET` | `/history?limit=50` | Recent processing history |
| `GET` | `/health` | Health check |

---

## PII Redaction Examples

| Input | Output |
|-------|--------|
| `My name is Priya Sharma` | `My name is [NAME]` |
| `Call me at 9876543210` | `Call me at [PHONE_NUMBER]` |
| `priya@example.com` | `[EMAIL]` |
| `Card: 4111 1111 1111 1111` | `Card: [CREDIT_CARD]` |

---

## Project Structure

```
Ghostdesk/
├── backend/
│   ├── main.py              # FastAPI application
│   ├── models.py            # LangGraph state & Pydantic models
│   ├── database.py          # SQLite operations
│   ├── workflow.py          # LangGraph workflow definition
│   └── agents/
│       ├── email_parser.py
│       ├── pii_redaction.py
│       ├── intent_classification.py
│       ├── attachment_analysis.py
│       ├── verification.py
│       ├── risk_detection.py
│       ├── resolution.py
│       └── human_approval.py
├── frontend/
│   └── app.py               # Streamlit UI (2 pages)
├── sample_data/
│   ├── test_emails/         # 3 demo email scenarios
│   ├── test_attachments/    # Generated sample PDFs
│   └── generate_sample_pdfs.py
├── requirements.txt
├── .env.example
└── README.md
```
